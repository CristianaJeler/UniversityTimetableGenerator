package com.scheduler.app.authentication.errors;

public enum AuthValidator {
    EXISTING_EMAIL,
    EXISTING_USERNAME,
    INVALID_EMAIL,
    INVALID_USERNAME,
    INVALID_PASSWORD,
    VALID_INPUTS
}
